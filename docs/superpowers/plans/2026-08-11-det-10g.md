# Det10g Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `Det10g : public Engine` mirroring Yolov5, with SCRFD postprocess + 5 landmarks for `model/det_10g.axmodel`.

**Architecture:** Protected `Engine::config_` for subclass defaults; Det10g converts packed `[N,C]` outputs to CHW and calls `GenerateProposalsScrfd` + `GetOutBbox`. Minimal Scrfd kps fix for anchor `q`.

**Tech Stack:** C++17, AX Engine, existing `detection` helpers

## Global Constraints

- Spec: `docs/superpowers/specs/2026-08-11-det-10g-design.md`
- Do not change InfProcess / main_rtsp / main_img
- Manual test by user; build must succeed
- Do not change Yolov5 default EngineConfig fire model

---

### Task 1: Engine config access

**Files:**
- Modify: `hal/ax/engine.h`

- [x] Move `config_` from `private` to `protected` (keep other members private)

### Task 2: Fix Scrfd kps anchor indexing

**Files:**
- Modify: `hal/ax/detection.cpp` (`GenerateProposalsScrfd`)

- [x] Index kps as `(q * 10 + c) * feat_size + index`

### Task 3: Det10g class

**Files:**
- Create: `nodes/det_10g.h`, `nodes/det_10g.cpp`

- [x] Ctor applies defaults to `config_`
- [x] Postprocess: 9 outputs, group by size, packed→CHW (interleaved `index*2+q`), Scrfd + GetOutBbox, set `model="det_10g"`

### Task 4: Build verify

- [x] `cmake --build build -j$(nproc)` succeeds
